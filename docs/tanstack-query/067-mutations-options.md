---
id: mutationOptions
title: mutationOptions
---

```tsx
mutationOptions({
  mutationFn,
  ...options,
});
```

**Opções**

Você geralmente pode passar tudo para `mutationOptions` que também pode passar para [`useMutation`](./useMutation.md).
